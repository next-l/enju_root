<%= form_for(@nomen) do |f| %>
  <% if @nomen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nomen.errors.count, "error") %> prohibited this nomen from being saved:</h2>

      <ul>
      <% @nomen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
