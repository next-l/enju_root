<%= form_for(@work_and_classification) do |f| %>
  <% if @work_and_classification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_and_classification.errors.count, "error") %> prohibited this work_and_classification from being saved:</h2>

      <ul>
      <% @work_and_classification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_id %><br>
    <%= f.number_field :work_id %>
  </div>
  <div class="field">
    <%= f.label :classification_id %><br>
    <%= link_to @work_and_classification.classification.term, @work_and_classification.classification %>
    <%= f.hidden_field :classification_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
